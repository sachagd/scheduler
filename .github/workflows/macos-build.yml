name: macOS build

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-macos:
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install build deps
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller

      - name: Build with PyInstaller
        run: |
          pyinstaller --onefile --name MeetingScheduler main.py
        # replace main.py with your script filename

      - name: Package artifact
        run: |
          cd dist
          # rename for clarity and zip it (optional)
          mv MeetingScheduler MeetingScheduler-macos
          tar -czf MeetingScheduler-macos.tar.gz MeetingScheduler-macos

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: MeetingScheduler-macos
          path: dist/MeetingScheduler-macos.tar.gz
